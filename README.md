# Aldrin Morris D. Balitbit's Website

This repository for myself uses [Jekyll]() to build the website using `rake` command.

- For production: `rake jekyll:build`
- For development: `rake jekyll:serve`
